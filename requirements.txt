fastapi==0.136.0
uvicorn==0.44.0
requests==2.33.1
dnspython==2.8.0
pydantic==2.13.2
beautifulsoup4==4.14.3
certifi>=2024.0.0

# Database (Supabase Postgres client + pure-Python driver for migrations)
supabase==2.28.3
psycopg[binary]==3.3.3

# PDF report export (Pro plan feature). Pure Python, no system deps —
# matters for the python:3.11-slim base image on HF Spaces.
fpdf2==2.8.7
