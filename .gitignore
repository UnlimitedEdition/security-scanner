# ==========================================================================
# Python
# ==========================================================================
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.Python
*.so
venv/
env/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ==========================================================================
# Secrets — NEVER commit these
# ==========================================================================
.env
.env.*
!.env.example
*.secret
*.key
*.pem
*.p12
*.pfx
credentials.json
service-account*.json
.claude/.credentials.json
secrets/

# ==========================================================================
# Backup / temp files — scanner flags these on other sites,
# so committing them here would be embarrassing
# ==========================================================================
*.bak
*.old
*.backup
*.swp
*.swo
*.tmp
*.orig
*~
b64_temp.txt

# ==========================================================================
# IDE / editor
# ==========================================================================
.vscode/
.idea/
*.sublime-*
.cursor/

# ==========================================================================
# OS artifacts
# ==========================================================================
.DS_Store
Thumbs.db
desktop.ini

# ==========================================================================
# Logs
# ==========================================================================
*.log
logs/
audit/*.log

# ==========================================================================
# Deploy
# ==========================================================================
.vercel
.netlify
.huggingface/

# ==========================================================================
# Local development databases
# ==========================================================================
*.sqlite
*.sqlite3
*.db
local.db
dev.db

# ==========================================================================
# AI agent local files (Claude Code / Codex) — never commit private agent state
# ==========================================================================
.claude/
.codex/
.playwright-mcp/
memory/
CLAUDE.local.md
CLAUDE.md
AGENTS.md
BLOG-TODO.md

# ==========================================================================
# code-review-graph — lokalni graph DB + wiki artefakti
# ==========================================================================
.code-review-graph/

# ==========================================================================
# Scan exporti — korisnički/interni scan rezultati ne idu u repo
# ==========================================================================
scan-*.json
scan-*.csv
json-*.json

# ==========================================================================
# Interni Lemon Squeezy listing materijali — NE commit-ovati.
# vercel.json ima "outputDirectory": "." i robots.txt ima "Allow: /",
# pa je svaki commit-ovan fajl javno dostupan na produkciji.
# .vercelignore je dodat 2026-08-09 kao drugi sloj zastite.
# ==========================================================================
lemon-images.html
lemon-product-*.png

# ==========================================================================
# Node (if frontend ever gets build tooling)
# ==========================================================================
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml
*.pdf
